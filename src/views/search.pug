extends base.pug
include mixins/video

block content
  form.search__form(method="GET")
    input(placeholder="Search by Title", name="keyword", type="text")
    button
      i.fas.fa-search

  .video-grid
    each video in videos
      +video(video)
    else
      span.empty__messagea No videos found
